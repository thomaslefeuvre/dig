version: '3'

vars:
  entrypoint: main.go
  output: bin/digg

tasks:
  clean:
    cmds:
      - rm -f {{.output}}

  build:
    deps:
      - clean
    cmds:
      - go build -o "{{.output}}" "{{.entrypoint}}"
